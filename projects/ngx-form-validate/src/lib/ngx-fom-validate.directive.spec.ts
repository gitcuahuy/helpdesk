import { NgxFomValidateDirective } from './ngx-fom-validate.directive';

describe('NgxFomValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFomValidateDirective();
    expect(directive).toBeTruthy();
  });
});
